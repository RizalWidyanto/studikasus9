#include <iostream>
class data {
  public:
    data i;
    i.inputoutput();
    i.proses();
  private :
    Kamar kmr[15];		// deklarasi array 1 dimensi (input)
    Kamar km[15][15];	// deklarasi array 2 dimensi (output)

    int n, jed, k, l;
struct Kamar{
  string Jenis ;
  string Judul ;
  string Penulis; 
};